{
  "type": "module",
  "name": "chrome-cool-copy",
  "version": "1.0.0",
  "description": "Chrome extension for enhanced copy functionality",
  "private": true,
  "scripts": {
    "build:chat": "pnpm --filter @repo/chat build",
    "build:extension": "pnpm --filter @repo/extension build",
    "build": "pnpm build:chat && pnpm build:extension"
  }
}
